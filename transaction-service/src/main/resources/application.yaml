spring:
  application:
    name: transaction-service     # MUST match "transaction-service.yml" in config repo
  config:
    import: "configserver:http://localhost:8888"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true