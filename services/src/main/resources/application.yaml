spring:
  application:
    name: services            # MUST match config file name "services.yml" in config repo
  config:
    import: "configserver:http://localhost:8888"   # fetch config from here

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka     # register here
  instance:
    prefer-ip-address: true